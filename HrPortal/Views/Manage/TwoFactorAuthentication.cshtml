<!DOCTYPE html>

<html lang="tr">
<head>
    <meta charset="utf-8">
</head>
<body>
    @model TwoFactorAuthenticationViewModel
    @{
        ViewData["Title"] = "�ki Fakt�rl� Kimlik Do�rulama";
        ViewData.AddActivePage(ManageNavPages.TwoFactorAuthentication);
    }
    <div class="container" style="margin-bottom:20px;">
        <div class="row">
            <div class="col-md-6">

                <h4>@ViewData["Title"]</h4>
                @if (Model.Is2faEnabled)
            {
                if (Model.RecoveryCodesLeft == 0)
                {
                        <div class="alert alert-danger">
                            <strong>Kurtarma Kodunuz Yok.</strong>
                            <p>You must <a asp-action="GenerateRecoveryCodes">Kurtarma kodu set olu�tur.s</a> Kurtarma modu ile girmeden �nce</p>
                        </div>
                    }
                    else if (Model.RecoveryCodesLeft == 1)
                    {
                        <div class="alert alert-danger">
                            <strong> Sadece bir tane kurtarma kodunuz kald�.</strong>
                            <p> <a asp-action="GenerateRecoveryCodes">Kurtarma kodlar� seti olu�tur.</a>.</p>
                        </div>
                    }
                    else if (Model.RecoveryCodesLeft <= 3)
                    {
                        <div class="alert alert-warning">
                            <strong> @Model.RecoveryCodesLeft tane kurtarma konuduz kald�..</strong>
                            <p> <a asp-action="GenerateRecoveryCodes">Yeni  kurtarma kodlar� seti olu�tur.</a>.</p>
                        </div>
                    }

                    <a asp-action="Disable2faWarning" class="btn btn-success">Pasifle�tir 2FA</a>

                    <a asp-action="GenerateRecoveryCodes" class="btn btn-primary">Kurtarma Kodlar�n� S�f�rla</a>

                }

                <h5>Kimlik Do�rulama Uygulamas�</h5>
                @if (!Model.HasAuthenticator)
            {
                    <a asp-action="EnableAuthenticator" class="btn btn-success">Kimlik Do�rulama Uygulamas� Ekle</a>
                }
                else
                {
                    <a asp-action="EnableAuthenticator" class="btn btn-primary"> Yap�land�r</a>

                    <a asp-action="ResetAuthenticatorWarning" class="btn btn-danger">S�f�rla </a>
                }
            </div>
        </div>
    </div>
</body>
</html>