@model IEnumerable<Job>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<section class="bg-alt">
    <div class="container">
        <header class="section-header">

            <h2>Güncel İlanlar</h2>
            <p>Son yayınlanan güncel ilanlar</p>
        </header>

        <div class="row item-blocks-condensed">
            @foreach (var Jobitem in ViewBag.jobs)
            {
                <div class="col-xs-12">
                    <a class="item-block" asp-action="Details" asp-route-id="@Jobitem.Id">
                        <header>
                            <img src="/uploads/jobs/@Jobitem.Company.Photo" alt="@Jobitem.Company.Name">
                            <div class="hgroup">
                                <h4>@Jobitem.Title</h4>
                                <h5>@Jobitem.Company.Name</h5>
                            </div>
                            <div class="header-meta">
                                <span class="location">@if (Jobitem.JobLocations != null)
                                {
                                    foreach(var location in Jobitem.JobLocations)
                                    {
                                        @location
                                        <text>, </text>

                                    }

                                }
                                </span>
                                <span class="label label-success">@Jobitem.WorkingStyle</span>
                            </div>
                        </header>
                    </a>
                </div>



            }
            <!-- Job item -->
       
            <!-- END Job item -->
            <!-- Job item -->
          

        </div>

        <br><br>
        <p class="text-center"><a class="btn btn-info" asp-controller="Jobs" asp-action="Index">Bütün ilanlara gözat</a></p>
    </div>
</section>
